version: "3"
services:
  frontend:
    build:
      context: frontend/
    ports:
      - 3000:3000
  backend:
    build:
      context: backend/
    ports:
      - 8080:8080
  db:
    image: mysql:latest
    environment:
      MYSQL_ROOT_PASSWORD: q1q1q1
