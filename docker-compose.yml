version: "3"
services:
  frontend:
    build:
      context: backend/
      dockerfile: Dockerfile
    ports:
      - 8080:8080
  backend:
    build:
      context: frontend/
      dockerfile: Dockerfile
    ports:
      - 3000:3000
  db:
    image: mysql:latest
    environment:
      MYSQL_ROOT_PASSWORD: db-57xsl
